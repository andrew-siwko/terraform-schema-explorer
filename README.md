# terraform-schema-explorer
Each provider version contains its own schema which may be extracted with `terraform providers schema -json`.  The terraform code contains references to a number of providers which we will interrogate and investigate.
